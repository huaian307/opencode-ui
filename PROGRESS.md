# PROGRESS.md — opencode-ui 历史沿革与原始实测证据

> ⚠ 本文件是**档案**，不是工作手册。
> 约定、架构、接口、踩坑、功能清单、验证方式、路线图 **均已合并进 `AGENTS.md`**（新会话自动加载）——
> 日常开发请看 `AGENTS.md`，不要从这里找现行约定。
> 这里只保留两样东西：① 项目是怎么一步步长成现在这样的（**演进史**）；
> ② 支撑那些结论的**未压缩原始实测数据**（考古 / 复盘时用）。
> 最后更新：2026-09-23（前端 `ui 260923h`）

---

## 当前进度总结（2026-09-23，前端 `ui 260923h`）

### 一句话

自建面板已经**能当日常界面用**，这一轮又把它从"能用"推到"有主题、能选模型、听得懂外文歌、会跟系统联动"：
会话管理、消息流式渲染、QQ音乐联动（真频谱 + 真实系统音量 + **歌词中文翻译**）、form/权限弹窗、
动态壁纸（WE 原片，**展开＝直接暂停当背景、不再截图**）、界面设置面板、
**模型切换（含官方品牌图标）**、**龙族视觉层**、**任务栏联动** 均已完成并实测通过。
中途按要求做过的**微信消息面板（截图 + OCR）已整体回滚**——现在代码里没有任何微信痕迹。

### 运行态（实测）

| 项 | 值 |
| --- | --- |
| 面板服务 | 8787（pid 13924）；`/healthz` → `"ui":"260923h"`，上游 OpenCode **2.0.13** |
| 守护进程 | 8788（pid 12836）；**任务栏联动在跑**：`ABM_GETSTATE=0x1`（自动隐藏开）、标记 `_taskbar.hidden` 在、日志有**双向**记录 |
| 系统音量 | 真值（Core Audio `IAudioEndpointVolume`，不是当年谎报 100 的 winmm） |
| 真频谱 | 48 段；采集设备 = `扬声器 (Realtek(R) Audio)`（"跟着默认输出设备走"在工作） |
| 动态壁纸 | 昼/夜由**用户在面板里选**（`_wallpapers.json`）；展开时**暂停视频本身**当静态背景 |
| 界面设置 | 背景亮度 昼 100% / 夜 80%、毛玻璃 18px、壁纸速度 0.80×、动态壁纸 开、落樱 开、**歌词翻译 开** |
| 模型 | 当前会话 `deepseek/deepseek-flash`；顶栏用**官方品牌图标**显示；`/api/model` 共 9 个（7 免费 / 2 付费，4 个带 variants） |
| 规模 | `app.js` 2198 行 / 86.6 KB · `style.css` 1238 行 / 53.1 KB · `index.html` 10.9 KB · `server.py` 1276 行 / 48.6 KB · `watch.py` 19.0 KB · `taskbar.py` 4.1 KB |

### 本轮（这个会话）做完的事

| # | 需求 | 结果 |
| --- | --- | --- |
| 1 | **任务栏联动** | 开面板 → 任务栏自动隐藏（鼠标贴底边才浮现）、关面板 → 恢复常驻；`taskbar.py` + `watch.py` 挂钩 + `stop.bat` 兜底；用**标记文件**区分"是不是我们藏的"，不覆盖用户自己的设置 |
| 2 | **模型切换** | 顶栏「模型」→ 按 provider 分组 + 可搜索；`POST /api/session/{id}/model`（**204**）；**variants**（思考强度）做成一排按钮；没选会话时存成"新会话默认模型"（`localStorage`） |
| 3 | **模型官方图标** | 查证：OpenCode 的 `/api/model`、`/api/provider` **都没有图标字段** → 从 models.dev / simpleicons / LobeHub 下载品牌标到 `web/assets/models/`（**本地素材**）；无品牌标（Muse Spark / Big Pickle）用**青铜字母徽章**兜底；顶栏从"显示名字"改成"**显示官方图标**" |
| 4 | **龙族视觉层** | 按原著设定做了一套界面语汇：**半朽的世界树校徽**（内联 SVG）、**青铜铭牌**按钮 + 四角刻线、**黄金瞳**滑块、设置项**罗马数字序列**、题字（装备部 / 言灵周期表 / 尼伯龙根） |
| 5 | **歌词中文翻译** | 听歌模式下在原文下面加一行**小字译文**；有道 demo 接口 + **7 天磁盘缓存**（`_lyrics_zh.json`）+ **中文歌自动跳过** + 设置里可关（`tr=0/1` 会重取歌词） |
| 6 | **动态壁纸"展开"改法** | 不再抓帧截图 → **直接暂停 `<video>`，停住的那一帧就是静态背景**；片头黑场改成"只量亮度、必要时 seek"，`#bg-freeze` 那一层彻底删掉 |
| 7 | 微信消息面板（截图 + OCR 只读、确认后回复） | 做完并验证后**按用户要求整体回滚**：`tools/wechat_*.ps1`、`#wx` 弹窗、`/wechat/*` 接口、`_wechat*.json` 全部删除；`check_features` 去掉 9 项微信断言（132 → 123 项） |

### 顺带修掉的真 bug / 踩到的真坑（都有实测）

1. **顶栏「模型」按钮被截断成 `deep`** —— `#topbar .actions button { width: 34px }` 赢了自适应宽度；改用 **id 选择器**放开（AGENTS 踩坑 #24）。
2. **`<img>` 里的 SVG 不继承页面颜色** —— DeepSeek 标是 `currentColor`、蚂蚁标根本没写 fill → 放进 `<img>` 渲染成**纯黑，夜主题下看不见**；改成下载时按品牌色写死（踩坑 #26）。
3. **无头 `--virtual-time-budget` 量视频画面会失真** —— 虚拟时间跑得比解码快 → `drawImage` 全黑、`currentTime` 卡住不动，极易误判成"渲染坏了"。画面类结论必须看**截图 + Pillow**（踩坑 #27）。
4. **探针注入脚本的两条铁律**（踩坑 #25）：全局 `var sleep` 撞上 `app.js` 的 `const sleep` → **整个 app.js 语法错误、界面全废**；用 `str.replace` 做占位符替换会误伤**属性名** → 拦截失效，探针页可能真的发 `/bye`。
5. **`ABM_GETSTATE` 是 `0x4` 不是 `0x2`** —— 用错常量会把"关"读成"开"（踩坑 #23）；另外 `StuckRects3` 注册表是**注销时才写**的，别当实时状态用。
6. **`.ps1` 里一个全角冒号就能把正则读坏**（PS 5.1 按 ANSI 读）—— 微信采集器里实测过，改用 `\uXXXX` 转义（代码已随回滚删除，教训留在 AGENTS）。

### 已知限制（别再重复提议）

- 页面里直接播 QQ音乐音频：**不行**（DRM + vkey）；让客户端播指定歌曲：`qqmusic://` **未注册**
- WE 的 **scene 类壁纸**（私有 `scene.pkg`）浏览器放不了 → 壁纸选择器只列 **video** 类型
- **微信**：4.x 是 **Qt 自绘界面**，UI Automation 里只有 2 个匿名 Pane → 只能截图 + OCR（有错字、改版即失效、且违反其用户协议）；**已按用户要求回滚，不再做**
- form/权限弹窗**只轮询当前会话**，别的会话里的提问不会弹
- 用无头浏览器打开**真面板页**会触发 `pagehide → /bye`，**真的会杀掉 OpenCode** → 必须用探针页
- 有道翻译是**公开 demo 接口**，可能限流；译文为**机器翻译**，仅供参考
- 「关面板 → 恢复任务栏」这条现场路径**不能随便验证**（`KILL_OPENCODE=True`，真关窗会连带杀掉当前会话）；要验证得先放 `no-kill` **并重启守护进程**

### 下一步（等用户发话）

- **智能体（agent）切换**（`/api/agent`、`POST /api/session/{id}/agent`）—— 模型切换已做，agent 还没
- 代码变更 / diff 面板（`/api/vcs/status`、`/api/vcs/diff`）；历史消息向上翻页（现在只有最近 80 条，接口支持 `cursor`）
- 工具输出实时流（消费 `session.tool.progress`）；终端（`/api/pty`）与文件浏览（`/api/fs/list`、`/api/fs/read/*`）
- 多会话标签页；弹窗改成轮询**所有**会话
- 动态壁纸：不播放时给采集/解码降频；换片时做交叉淡入
- 真频谱 CPU 优化（不播放时降频或暂停，现在常驻约 20% 单核）
- 那 168 MB 的 Edge AI 视觉模型（删了 Edge 以后会重新下载）

### 质量基线（大改前端后必跑）

```bat
python tools\jsbalance.py web\app.js     :: 括号配平 + 函数重名
python tools\check_ids.py                :: DOM id 引用
python tools\check_features.py           :: ★ 123 项能力逐条断言（app.js + index.html + style.css）
python tools\inspect_ui.py               :: ★ 无头渲染看真实 DOM 顺序（--shot 出图）
```
本轮这四项 **全部 PASS**（`check_features` **123 项、缺失 0**）；`/healthz` → `ui 260923h`。

---

## 一、缘起与目标（演进史）

用户想在**不修改 OpenCode 官方桌面版**的前提下，拥有一个**外观完全自控**的 OpenCode 界面，
并且希望它跟 OpenCode 自身**成对启停**（OpenCode 开了就拉起面板，面板关了就把 OpenCode 关掉）。

按用户提出的顺序演进：

1. 「你能修改你自己的 UI 界面吗？」→ 结论：Electron 的界面编译在 `app.asar` 里，
   官方没有自定义入口；改 asar 会被更新覆盖、还会破坏签名。→ 于是**自建界面**
2. 「就按方案 a 来吧」→ 选定**零依赖方案**：Python 标准库代理 + 原生 JS 前端
3. 「我需要一个龙族里绘梨衣风格的 UI」→ 确定视觉主题
4. 「先给我看再决定是否加入 UI」→ 做参考图筛选流程（含 Pixiv 可达性勘查）
5. 「Q 版的」→ 第二轮专项搜图（Q 版/chibi）
6. 一系列交互与体验修复（实时同步、方向、壁纸、侧栏、水花…）
7. 「能直接在个人界面调用 QQ音乐吗？」→ 接入 Windows 媒体会话（SMTC）做播放控制 / 歌词 / 音量，
   但**播放音频本身做不到**（DRM + `qqmusic://` 未注册），已如实说明边界
8. 「给我一个选项」→ 发现 **form / 权限弹窗必须自己做**，否则 agent 会被静默卡住 → 完成并实测通过
9. 「能实现真频谱吗」→ 用 WASAPI 回环 + FFT 做出**真音频条**
10. 收官：给前端加**版本自检 + 自动刷新**、给项目加**功能体检 / 无头渲染查看**两个工具
    （起因是一次严重事故：用 PowerShell 重写 UTF-8 文件毁掉了整个 `app.js`，只能整体重写，见「三」）
11. 「侧边栏好像不能删除会话」→ 查证：后端 `DELETE /api/session/{id}` 一直可用（204），
    是**前端从来没有入口**。补上会话项右侧的 × （平时淡、悬停变红）+ 二次确认；
    删掉的若是当前会话，则清空界面并自动选剩下最新一条。
12. 「每次新建都是新会话，不好管理」→ 改成**用第一句提问命名**：
    接口是 `PATCH /api/session/{id}` body `{"title":"..."}`（204，从 openapi.json 查证并实测）。
    只在该会话**标题还是默认值且还没有用户消息**时才改，并用 `state.named[id]` 记账只命名一次，
    用户手动改过名的绝不覆盖。标题压平空白、30 字截断。
    实测：输入 `"  帮我看看 ## 这个项目的  目录结构，顺便把 README 也过一遍，谢谢！"` →
    标题 `帮我看看 ## 这个项目的 目录结构，顺便把 README…`（前后端一致，第二句不再改名）。
13. 「动态壁纸：收起侧栏时动、展开时静，样式参考 Wallpaper Engine」→ 先去查证 WE：
    31 个壁纸里 **27 个是 scene 类型**（私有 `scene.pkg`，浏览器放不了）、
    场景壁纸的 `preview.gif` 只有 **160~224 px**（糊）、机器上**没有 ffmpeg**。
    第一版按"不装东西"做了**纯 CSS** 动态层（推拉/视差/光斑/浮尘/光带），
    实测六层确实在动（57/57 帧 transform 逐帧不同），但用户反馈"**没动起来**"——
    96 秒周期的推拉幅度 1.04→1.10，一次注视根本看不出差别（教训：**自证要挑"人眼尺度"的指标**，
    不要只证明"值在变"）。
    第二版改用 **WE 的 mp4 原片**：昼 `3314492008` 绘梨衣 4K60、夜 `3329723405/files` 三叶 4K
    （即预设「白天绘梨衣，晚上三叶」；用户说的"夜晚有原图"就是这个 4K 原片）。
    为了不复制、不转码 320/410 MB 的大文件，`server.py` 新增
    `GET /live/wallpapers`（glob 找片）+ `GET /we/<路径>`（**支持 Range 的流**），
    并把 `_send_file()` 抽成共用 —— `_static` 也顺带获得了 Range。
    实测：Range → `206 Content-Range: bytes 0-1023/336235138`；4K60 播 5.8s / 349 帧 / **丢 4 帧(1.15%)**；
    展开 `paused=true`（不再解码）。真实画面存成 `_shots/live-frame-hiru.jpg` / `live-frame-yoru.jpg`。
    纯 CSS 那套**保留为兜底**（找不到 Steam / 视频加载失败时自动退回）。
14. 「我新增了月读命绘梨衣和绯莎绘梨衣，做成夜/昼的动壁」→ 重新扫库（33 个），两个都是 **video** 类型：
    昼「［绯莎］绘梨衣 sakura」(`3786830950`, 4K, 64.7MB) / 夜「月读命绘梨衣」(`3568633530`, 4K, 55.7MB)。
    踩到的点：**夜主题那个 mp4 的文件名是一串哈希**（`a9dc0adb….mp4`）→ 按文件名匹配行不通，
    于是把 `WE_WALLPAPERS` 改成支持 `title:关键字`（扫 `project.json` 的标题，最稳）/ `id:文件夹` / glob 三种候选。
    另加两条体验：`/live/wallpapers` 返回 `detail{title,file,size_mb}` 方便核对选中了哪张；
    前端**每 5 分钟重拉一次清单**，新订阅的壁纸不用手动刷新。
    实测：两张都是 3840×2160，Range 206，收起在播（昼 371 帧丢 3 / 夜 176 帧丢 0），展开 paused。
    （夜那张原片自带作者水印，是壁纸内容本身。）
15. 「动态壁纸播放速度稍快」→ `app.js` 加 `LIVE_RATE = 0.8`，在 `sync()` 里对 `video.playbackRate`
    每次同步都设一遍（免得被重置）。实测证据：真实时间过 6.00s、视频只走 4.80s → **实测倍速 0.80**；
    展开后仍 `paused=True`。（想再慢就调这个常数，别低于 0.5。）
16. 「能不能接入 wallpaper 让我自己选壁纸」→ 做了**面板内选择器**（顶栏「壁」按钮）：
    `GET /live/list` 列出所有 **video 类型**壁纸（scene 放不了，不列；缩略图用壁纸自带的 `preview.gif`），
    `POST /live/pick {theme,id}` 指派给昼/夜并**落盘 `_wallpapers.json`**，
    优先级「用户选择 > `WE_WALLPAPERS` 默认候选」，选完立刻换片。
    实测：pick → `/live/wallpapers` 立刻改指；`id:null` → 恢复默认；面板里 4 条 × 2 按钮、缩略图已加载。
    踩坑（已记 AGENTS #21）：按钮上写了 `data-theme="yoru"`，结果**继承了夜主题的 CSS 变量**、
    按钮变黑 —— 属性改名 `data-slot` 解决。
    另：复现测试时发现 `_wallpapers.json` 里已有一组选择（白昼=4K60绘梨衣、夜晚=Kuroha），
    是**用户自己在面板里点的**（我的测试只动过 yoru 且会复位）—— 说明选择器在真实面板里已跑通。
17. 「动态壁纸要缓慢启动、展开时缓慢停止并把停下那一帧当静态背景；没有动态壁纸时两侧都用静态壁纸」
    → 重做这一块：
    收起 = 淡入（opacity .55s）+ `playbackRate` 0.1→0.8 线性加速（1.6s）；
    展开 = 0.8→0.1 线性减速（1.2s）→ `pause()` → canvas 抓下这一帧 → 贴到新建的 `#bg-freeze`
    → 再淡出到 0；两侧画面因此完全一致，只是动与不动。
    **删掉**了原来那套纯 CSS 动态层（推拉/视差/光斑/浮尘/光带 —— 用户反馈"没动起来"，
    96 秒周期根本看不出）；没有原片时两侧都用主题自带的静态壁纸。
    实测（时间线，真实时间）：收起 0.22→0.35→0.49→0.61→0.75→**0.80**、透明度 0→1；
    展开 0.63→0.47→0.29→**0.12 → 停**、透明度 1→0；`has-freeze=True`、`body::after=none`。
    踩坑（已记 AGENTS #22）：起步速率写 0.06 → `NotSupportedError`（下限 0.0625）→
    **整个收起流程静默中断**，表现是"收起后完全没反应"；静态检查抓不到，
    靠 `window.onerror` + 时间线才定位。改 0.3 并给所有赋值加 try/catch。
18. 「启动太慢了，而且昼夜切换会有一大块黑屏」→
    ① 启动：`LIVE_START_RATE` 0.1→**0.3**、`LIVE_RAMP_IN` 1600→**600ms**、淡入 .55→.35s
    （实测 755ms 到正常速度）。
    ② 黑屏根因：换主题时会立刻换 `src` 并丢掉旧冻结帧，而**新视频还没解码出帧** ——
    `<video>` 无帧时**画黑**，盖住下层静态壁纸 → 一大块黑。而且我原本还给它加了 .25s 过渡，
    等于"带着黑慢慢淡出"，仍有 250ms 黑。
    → 采用用户提的思路：换片时先 `remove("ready")` **立刻把视频藏起来**（CSS 上"藏"不设过渡），
    由 `#live-bg` 自带的主题静态壁纸顶上；`loadeddata` 拿到**第一帧**就 `capture()` 存成该主题的
    静态背景并 `.ready` 淡入 —— "先用新片第一帧占位，之后再被暂停帧替换"。
    ③ 冻结帧改成 `{url, data}`：只有帧所属的片子 == 当前主题该用的片子时才用，避免张冠李戴。
    实测不变量：**全程 0 帧"视频可见但还没帧"**；切到夜主题后 `has-freeze` 自动恢复（21 帧里 19 帧为真）。
19. 「在会话界面切换主题会黑屏」→ 用**虚拟时间截图**复现：切到夜主题后 6 秒，会话区**仍是全黑**
    （截图从 616KB 掉到 163KB，一眼看出画面没了）。dump 状态才看清真相：
    `has-freeze=True`、`body::after=none`、`body-image = data:image/jpeg…`、而 `night.jpg` 本身能加载（1000x563）。
    → **黑的是"抓下来的那一帧"**：换主题时新视频刚 loadeddata，我抓的是**第 0 帧**，
    而动态壁纸片头常是**淡入黑场** → 黑帧被当成静态背景，又把 `body::after` 顶掉 → 永久黑底。
    修法：`drawFrame()` 量帧平均亮度（`< 12` 视为黑场），`grabFrame()` **先用停下来的那一帧**，
    太黑才 seek 到 1.0/2.5/4.0s 重试；**全不行就不设冻结帧**（退回静态壁纸）——
    任何情况下都不会再有黑底。回归：收起播 3.4s → 展开停在 4.62s，冻结帧 101KB 的 dataURL ✓。
    （第一次修还踩了个小的：我把 seek 放在抓帧**之前**，于是"停下来的那一帧"变成了 1.0s 那帧；
    改成"先抓当前帧、黑了才 seek"才对。）
20. 「给会话界面的背景降低亮度」→ 会话界面的背景是 **`#bg-freeze`（冻结帧）**，它**没有任何蒙层**，
    所以比带纸色蒙层的静态壁纸更亮更冲。加一个 CSS 变量 `--bg-dim`（昼 `.85` / 夜 `.78`），
    `#bg-freeze` 与 `body::after` 都套 `filter: brightness(var(--bg-dim))`，两条路观感一致。
    验证用**同一个探针做 A/B**（把 `--bg-dim` 内联设成 1 vs 默认）再各截一张图，
    用 Pillow（借 `lession/.venv`）量"背景条"亮度：昼 228.6→195.8（−14.6%）、夜 44.3→34.6（−21.9%），
    正好对上 0.85 / 0.78。⚠ `check_features` 只扫 `app.js`，**纯 CSS 的改动它断言不了**（我加错过一条，已撤）。
21. 「白昼静态背景又太暗了；加个设置按钮，把亮度等设置放进去」→
    ① 亮度改成**按主题分开**的变量：`--bg-dim-hiru`（默认 **1** = 原样）/ `--bg-dim-yoru`（默认 **0.8**），
    `--bg-dim` 按主题取其一，所以 JS 只要覆盖这两个变量、切主题时不用重算。
    实测白昼默认回到 1.00 后背景条亮度 **229.4 == 原亮度 229.4**。
    ② 新增 **设置弹窗**（顶栏「设」）：背景亮度（昼/夜两个滑块）、动态壁纸速度（`liveRate` 从常量改成可调）、
    收起时是否播放动态壁纸、落樱、「选壁纸…」、恢复默认；全部存 `localStorage` 并立刻生效
    （`SET` + `applySettings()` 写内联变量、切 `body.no-petals`、调 `liveApply()`）。
    顺带把 ❀ 落樱按钮也接到这套设置上（两处状态不再各说各话）。
    ③ 新踩的坑：**开着弹窗截图量背景亮度会被遮罩污染**（量到 163.8，误以为没生效）——
    量背景必须"不开弹窗"再来一张。
22. 「给会话界面的背景加高斯模糊，像盖了层毛玻璃」→ 加 `--bg-blur`（默认 `18px`）：
    `#bg-freeze` 常套，`body::after` **只在 `html[data-sidebar="open"]` 时**套
    （收起听歌那一侧保持清晰，不模糊壁纸）；顺手把它也放进设置面板（0–60px 滑块）。
    踩到的细节：**模糊会把图层边缘和"透明"混合，四边出现一圈淡边** →
    开模糊时给两层 `inset: -3vmax` 撑出视口（`html.bg-blurred` 类控制），模糊关掉就不撑
    （不撑时取景与视频层完全一致，切换不会有一点位移）。
    验证：同一探针 A/B，用 Pillow 量背景区"清晰度"（相邻像素平均绝对差）——
    昼 2.14→0.43（**−80%**）、夜 0.60→0.16（**−74%**），并把截图存成 `blur-*-on/off.png`。

---

## 二、原始实测证据（未压缩）

### 后端接口 / 鉴权

- 鉴权试错：Bearer、`x-opencode-password`、`x-opencode-auth` **全部 401**，只有 **HTTP Basic** 通。
- 密码来源 `C:\Users\TWG\.local\state\opencode\service.json` → `{url, password, version, pid}`；
  `url` 里的端口每次重启都可能变（`server.py` 每次读文件）。
- 附件：`{"uri":"data:image/png;base64,...","name":"x.png"}` → **200**，落库为
  `{"data":"...","mime":"image/png","source":{"type":"inline"},"name":"x.png"}`（图片与 `text/plain` 都实测通过）。
- `GET /openapi.json` 共 **113** 个接口。

### 消息顺序 / 流式（关键实测）

- `/api/session/{id}/message` 返回顺序：`idx0 created=1790078977488` 起**逐项递减** → 确认是**新→旧**。
- 一轮对话的 SSE 事件量：
  `server.connected` **1**、`session.reasoning.delta` **70**、`session.text.delta` **32**、
  `session.tool.called/success` 各 **4**、`session.step.started/ended` 各 **4-5**、`session.usage.updated` **6**。
- 生成期间轮询 `/message`：`t=0s 文本 0 字 → t=3s 0 字 → t=6s 225 字`（**一次性出现**）
  → 证明该接口**不暴露生成中内容**，实时只能用增量事件。

### form / permission（探明过程）

- `GET /api/session/{sid}/form` → `{data:[{id, sessionID, title, fields}]}`。
  选择题的选项挂在 **`string` 字段的 `options[{value,label,description}]`** 上，`type` 不一定变成 `multiselect`。
- `POST /api/session/{sid}/form/{fid}/reply` body `{"answer":{<字段key>:<值>}}` → **204**；
  对**已结算**的表单再回复 → **409 `FormAlreadySettledError`**。
- `GET /api/session/{sid}/permission` → `{data:[{id, sessionID, action, resources, message}]}`；
  回复 `{"decision":"once"|"always"|"reject"}`。
- 实测第一个问题成功回收答案（见 AGENTS.md 里记的两处前端修复）。

### 真频谱（WASAPI 回环 + FFT）

```
播放前          bars = 全 0
播 440Hz 正弦   max=1.00  bars=0.68 0.61 0.60 0.79 1.00 1.00 0.35 0.22 0.08 ...
                           ↑ 峰值正好落在 440Hz 对应的那一格（40~16000Hz 对数分频）
播放结束        max=0.06 → 0.00                     ← 快起慢落（上升 0.60 / 下降 0.13）
端到端链路      采集 → _spectrum.json → /qq/spectrum → 前端柱高
```

> ⚠ **上面这份是 2026-09-22、`BANDS = 12` 时的原始记录**（当时用的是「频段内均值 + (db+62)/62」）。
> 2026-09-23 因「音频条起伏太小」做了两处改动，结论已变，旧数字不可直接对照：
> ① `BANDS` 12 → **48**；② 归一化改为「**频段内峰值** + `REF = BLOCK/4` + `DB_FLOOR = 50`」，
> 因为均值会把窄频段读高、中等音量就 clip 到 1.0（"一点声音就拉满"）。
> 改动后的实测（同一手法，播 440Hz）：`#20=0.91  #19=0.85  #21=0.27  #18=0.14` —— 尖峰。
> 另：`ATTACK/RELEASE` 未动，**快起慢落仍是 0.60 / 0.13**。
> 采集器本体常驻 CPU 实测约 **22% 单核**（48 段），比当年记的 14% 高。

### 「音频条不跳 / 音量拖不动」的两次实测定位（2026-09-23）

用户报「音频条不随音乐跳动、播放条音量也控制不了真音量」，查下来是**两个独立的真 bug**，都记在 AGENTS 踩坑 #19 / #20：

```
① 音量：waveOutGetVolume(NULL) -> 0xFFFFFFFF（不受支持），而真实系统音量是 15
   → 旧代码永远报 100、拖动无效。改用 Core Audio IAudioEndpointVolume（纯 ctypes）后：
     读 15 → 设 30 → 读 30 → 恢复 15                [OK]

② 频谱：蓝牙耳机成为默认输出后，音频条一直贴基线
   按名字取回环  -> 抓到同名"麦克风"端点，峰值 0.00003（静音）
   按 id 取回环  -> 峰值 0.29492                    [有信号]
   根因：蓝牙耳机的播放/麦克风端点同名（都叫「耳机 (YSX6)」），
        sc.get_microphone(名字) 会匹配错；以前 Realtek 名字不同所以没暴露。
   附带：采集器只在启动时定设备，切到蓝牙后会一直采旧设备的静音，
        且它仍在写文件 → 只看文件新鲜度的 keeper 永远不重拉。

修完后端到端：默认设备 = 耳机 (YSX6)，播 440Hz → 峰值落在**第 20 段**（0.75）；
浏览器真实时间跑 14s（页面每 400ms 把柱子高度回传）：宽带噪声下 23 帧有大量柱子高于基线、
18 次相邻帧变化 → 确实在跳。
```

### 关窗联动时序（`WM_CLOSE` 模拟点 X，挂 `no-kill`）

```
/alive 变 false     0.02s    页面 /bye 即时
守护进程发现关窗     0.10s    TICK 0.4s
反悔期              1.006s    设定 1.00s
从关窗到联动总计     1.05s
```

强杀窗口（崩溃 / `Stop-Process -Force`）时页面来不及发 `/bye`，只能等心跳超时 → 约 **8s**。

### `browser-profile/` 清理明细（481 MB → 244 MB，释放 237.7 MB）

`component_crx_cache` **183 MB** · 各类 Cache **44 MB** · Service Worker **22 MB**。
只删纯缓存，`Local Storage` / `Preferences` 一律不动 → 主题、侧栏状态、左上角名字都保留。
剩余 **168 MB** 是 Edge 自己的 AI 视觉模型 `ProvenanceData/…/vti-b-p32-visual.quant.ort`，**不是缓存，未动**。

---

## 三、事故档案：一次不可逆的前端损坏（2026-09-22）

改版本号时图省事写了
`(Get-Content web\app.js -Raw) -replace ... | Set-Content web\app.js -Encoding UTF8`。

- `Get-Content -Raw` **不带 `-Encoding` 时按 ANSI(GBK) 解码 UTF-8** → 中文全变乱码；
- 更要命：GBK 是**双字节**编码，字节错位会**吞掉换行** → `{ depth: 12 BAD`，**12 处括号不配平**；
- **809 个字符不可逆丢失**（尝试"乱码→按 GBK 编回→按 UTF-8 解码"的反变换，失败：
  字节里已经被写进了 `?`，信息真的没了）；
- `web\app.js`（1516 行）与 `web\index.html` 双双报废 → **只能整体重写**。

**后续影响**：整体重写时把「消息按 `time.created` 升序重排」弄丢了，用户又看到"对话从下到上"——
而 `jsbalance.py` 只看括号、`check_ids.py` 只看 DOM id，**都抓不到这种回归**，
因此后来新增了 `tools\check_features.py`（56 项能力逐条断言）。完整教训见 `AGENTS.md` 踩坑 #11 / #15。

---

## 四、已删除 / 已放弃的产物

- `web/app.js.corrupt.bak`（64 KB，上述事故的损坏样本）—— 已经用户确认**删除**。
- `_refs*/` 参考图（86 文件 / 32 MB）—— 一次性筛选产物，**用户要求保留**。
- `_shots/` 截图 —— 诊断产物，可随时删。

---

## 五、去哪里找什么

| 想了解 | 看这里 |
| --- | --- |
| 项目约定、环境硬约束、运行/停止/开关 | `AGENTS.md` |
| 架构与数据流、`watch.py` 联动状态机 | `AGENTS.md` |
| 后端 API、消息结构、SSE、附件、form/权限 | `AGENTS.md` |
| QQ音乐联动、真频谱（**接口与边界**） | `AGENTS.md` |
| 踩坑清单（27 条，含只对之后启动的守护有效的 `no-kill`） | `AGENTS.md` |
| 验证工具与「按对象选手段」 | `AGENTS.md` |
| 文件地图、规模与体积、路线图、明确排除项 | `AGENTS.md` |
| 项目**为什么**长成现在这样、原始实测数字、那次事故的细节 | **本文件** |
